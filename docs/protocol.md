# Protocol

The canonical protocol document is maintained at the repository root:

- `PROTOCOL.md`

It defines:

- manifest format and discovery
- lifecycle semantics (`managed` vs `standalone`)
- status/config/events endpoints
- AI conversation and SSE streaming contract

When implementing a new agent, start with `PROTOCOL.md` and the reference examples under `examples/`.
